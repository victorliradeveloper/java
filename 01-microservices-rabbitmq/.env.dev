# Valores de DESENVOLVIMENTO. NAO COMMITAR (ja esta no .gitignore).
# Carregar com: docker-compose --env-file .env.dev up

# Ativa o profile do Spring (carrega application-dev.yml).
SPRING_PROFILES_ACTIVE=dev

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=victorliradev@gmail.com
SMTP_PASS=mwidtdffpitevcyo
NOTIFICATION_MAIL_TO=victorliradev@gmail.com
