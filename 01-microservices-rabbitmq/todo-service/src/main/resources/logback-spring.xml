<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging por perfil:
    - dev/default: console legivel por humano, com [traceId,spanId] e idempotencyKey
      do MDC inline no fim da linha.
    - prod: uma linha JSON por evento (LogstashEncoder) com MDC promovido a campos
      de topo (traceId, spanId, idempotencyKey) — pronto pra ingestao em ELK/Loki.

  Os niveis (logging.level.*) continuam vindo do application*.yml; o Spring Boot
  os aplica por cima desta config.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="todo-service"/>

    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%X{traceId:-},%X{spanId:-}] [%X{idempotencyKey:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>idempotencyKey</includeMdcKeyName>
                <customFields>{"service":"${appName}"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
